id: event_format
label: 'Taxonomy: Event Format'

source:
  plugin: taxonomy_sql
  key: cherity
  vocabulary: event_format

process:
  vid:
    plugin: default_value
    default_value: event_format
  name: name
  description/value: description__value
  description/format:
    plugin: default_value
    default_value: basic_html
  weight: weight
  parent: parent

destination:
  plugin: entity:taxonomy_term
